@model IEnumerable<EMPDetails.Models.USerInfo>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table" border="1" style="border:solid black;border-collapse: collapse">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.empid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.empname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.empAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.empDept)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.empMobile)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.empid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.empname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.empAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.empDept)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.empMobile)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.empid }) |
            @Html.ActionLink("Details", "Details", new { id = item.empid }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.empid })
        </td>
    </tr>
    }
    
    </table>
</body>
</html>
